on: [push, pull_request]
name: Test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6.0.3
    - name: Install Go
      uses: actions/setup-go@v6.4.0
      with:
        go-version: 1.26.x
    - name: Run tests
      run: make race-test
